---
description: Wrapper for shared Figma design-system rules in context/rules.
globs: ["app/**/*.tsx", "app/**/*.ts", "app/**/*.css", "components/**/*.tsx", "components/**/*.ts", "src/**/*.tsx", "src/**/*.ts", "src/**/*.css"]
alwaysApply: false
---

# Figma MCP Design System Rules (Cursor Wrapper)

This Cursor wrapper preserves scoped rule application via `globs`.

Load canonical shared rules:
`/context/rules/figma-design-system.md`
